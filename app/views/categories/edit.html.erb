<% if user_signed_in? && current_user.admin? %>
<div class="w3-row w3-center w3-border w3-border-dark-grey">
  <div id="Pizza" class="w3-container menu w3-padding-32 w3-sand">
<h1>Editing Category</h1>

<%= render 'form', category: @category %>
</br>

<%= link_to 'Show', @category, :class => "button", :role => "button" %> 
<%= link_to 'Back', categories_path, :class => "button", :role => "button" %>
<%= link_to 'Destroy', @category, method: :delete, data: { confirm: 'Are you sure?' }, :class => "button", :role => "button" %></td>
   </div>
</div>
 <% else %>
<div class="w3-row w3-center w3-border w3-border-dark-grey">
  <div id="Pizza" class="w3-container menu w3-padding-32 w3-red">
    <h1>Sorry, you can not edit categories!!!</h1>
  </div>
</div> 
<% end %>