<div class="w3-row w3-center w3-border w3-border-dark-grey">
  <div id="Pizza" class="w3-container menu w3-padding-32 w3-sand">
    <h1>Choose from our popular meals!</h1>
    
      <% @items.limit(3).order(:id).each do |item| %>
      
          <div class="title">
            <%= link_to item.title, item %>
          </div>
          <div class="description">
            <%= item.description %>
          </div>  
          <div class="price">
             <%= number_to_currency(item.price, :unit => "€") %>
          </div>
          <div class="links">
            <a href="/cart/<%= item.id %>" class="button">Add to Cart</a>
        </div>
    <% end %>
   
     <%= image_tag("restaurant.jpeg", alt: "popular", :class => "popular") %> 
 </div>
</div>
