<% if user_signed_in? && current_user.admin? %>
<div class="w3-row w3-center w3-border w3-border-dark-grey">
  <div id="Pizza" class="w3-container menu w3-padding-32 w3-sand">
<h1>Editing Booking</h1>

<%= render 'form', booking: @booking %>
</br>

<%= link_to 'Show', @booking, :class => "button", :role => "button" %> 
<%= link_to 'Back', bookings_path, :class => "button", :role => "button" %>
<%= link_to 'Destroy', @booking, method: :delete, data: { confirm: 'Are you sure?' }, :class => "button", :role => "button" %></td>

  </div>
</div>
<% else %>
<div class="w3-row w3-center w3-border w3-border-dark-grey">
  <div id="Pizza" class="w3-container menu w3-padding-32 w3-red">
    <h1>Sorry, this section is only for Admin!!!</h1>
  </div>
</div> 
<% end %>
 
   